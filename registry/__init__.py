"""Authoring format for the contract: the Pydantic registry and its generator.

Dev-only. Nothing here is packaged — see ``python/pyproject.toml`` for why the
published wheel carries generated JSON and a plain-Python interpreter instead.
"""
